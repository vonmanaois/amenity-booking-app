import { SectionShell } from "@/components/layout/section-shell";

export default function AdminSettingsPage() {
  return (
    <SectionShell
      eyebrow="Staff Admin"
      title="Settings"
      description="Booking policies, cutoffs, and global staff-level configuration will be handled here."
    />
  );
}
