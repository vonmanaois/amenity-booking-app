import { SectionShell } from "@/components/layout/section-shell";

export default function AmenitiesPage() {
  return (
    <SectionShell
      eyebrow="Resident"
      title="Amenities"
      description="Browse active amenities, check rules, and enter the booking flow."
    />
  );
}
