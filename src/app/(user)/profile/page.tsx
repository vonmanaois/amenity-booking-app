import { SectionShell } from "@/components/layout/section-shell";

export default function ProfilePage() {
  return (
    <SectionShell
      eyebrow="Resident"
      title="Profile"
      description="Account settings and resident profile management will live here."
    />
  );
}
