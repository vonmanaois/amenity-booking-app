import { SectionShell } from "@/components/layout/section-shell";

export default function BookingsPage() {
  return (
    <SectionShell
      eyebrow="Resident"
      title="My Bookings"
      description="Residents will view upcoming, past, and cancelled bookings here."
    />
  );
}
