import { SectionShell } from "@/components/layout/section-shell";

export default function SignInPage() {
  return (
    <SectionShell
      eyebrow="Auth"
      title="Sign in"
      description="Authentication UI and role-aware redirects will be wired in next."
    />
  );
}
