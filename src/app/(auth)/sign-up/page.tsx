import { SectionShell } from "@/components/layout/section-shell";

export default function SignUpPage() {
  return (
    <SectionShell
      eyebrow="Auth"
      title="Sign up"
      description="Resident registration flow placeholder. This will be replaced once Auth.js is connected."
    />
  );
}
