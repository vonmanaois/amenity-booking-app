import { SectionShell } from "@/components/layout/section-shell";

export default function SuperuserDashboardPage() {
  return (
    <SectionShell
      eyebrow="Superuser"
      title="Overview"
      description="Escalations, staff governance, and platform-wide signals will be surfaced here."
    />
  );
}
